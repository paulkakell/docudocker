# Automated tests

The automated test suite covers:

- Configuration validation and error cases.
- Formatting utilities for release parsing, byte conversion, uptime formatting, and network name extraction.
- Service-layer transformation logic from Docker JSON to UI rows.
- Route rendering, security headers, auth behavior, and Docker error mapping.
